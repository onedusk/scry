# Environment variables for scry. Copy to .env and fill in, or export in your
# shell — scry reads these from the process environment.

# Required only when the manifest sets changelog_page_urls or design_system_urls
# (those pages are scraped via Firecrawl). Get a key at https://firecrawl.dev
FIRECRAWL_API_KEY=
